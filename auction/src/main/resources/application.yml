server:
  port : 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/auctions
    username: Zachary
    password: eecs4413
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show_sql: true
  main:
    web-application-type: servlet